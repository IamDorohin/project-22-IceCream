 import { tns } from "tiny-slider"

  const reviewsSlider = tns({
    container: '.reviews-slider',
    controls: false,
    items: 1,
    slideBy: 'page',
    autoplay: false,
    navPosition:'bottom',
  });